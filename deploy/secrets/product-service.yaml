# deploy/secrets/product-service.yaml → vault:secret/product-service | asm:app/product-service
#
# Nothing here moves with the environment: Kafka topic and consumer-group names
# are part of the application's contract with its peers, and the port is fixed
# by the gateway route and the Service definition. Transcribed from the golden
# capture of the three old seeding paths; the equivalence test, not this file,
# is the authority on whether a value is right.

application.kafka.group-id.product-service.product.update-quantity: product-service.product.update-quantity
application.kafka.topics.inventory-service.product.update: inventory-service.product.update
application.kafka.topics.product-service.product.update-quantity: product-service.product.update-quantity

server.port: "7777"
