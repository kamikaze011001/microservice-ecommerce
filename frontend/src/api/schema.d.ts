// Placeholder — replace with `pnpm api:gen` output once gateway is reachable.
// Backend was unavailable during Phase 3 implementation (Atomikos MySQL pool
// failure on local stack); plan Task 3 explicitly permits this stub.
export type paths = Record<string, never>;
export interface components {
  schemas: Record<string, unknown>;
}
