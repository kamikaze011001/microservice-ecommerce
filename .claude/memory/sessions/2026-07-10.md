# 2026-07-10

## Done
- Ran `/doctor` (harness health check). Installation clean: native install, `PATH` correct,
  `installMethod` agrees, no npm leftovers, all settings files parse, no agent definitions.
  Version 2.1.206 (ahead of the 2.1.205 `latest` channel). Auto mode already the default.
- **Trimmed root `CLAUDE.md`: 18,704 → 14,452 chars** (~4.7k → ~3.6k est tokens resident every
  session). Cut six derivable sections; migrated three module-scoped ones out.
- **Created `mock-paypal-service/CLAUDE.md`** (Java 25 toolchain, `application.paypal.base-url`
  switch, `?decision=` / HTTP 422 semantics, single-replica constraint) and cross-linked it from
  `payment-service/CLAUDE.md`.
- Verified before removing: image-storage content is duplicated in `core/core-s3/`,
  `product-service/`, and `authorization-server/` CLAUDE.md; CORS in `gateway/CLAUDE.md`.

## Decisions
- `decisions/0002-root-claude-md-delegates-to-nested-module-files` — root holds only non-derivable,
  cross-cutting guidance; module specifics live in nested files.
- `conventions/nested-claude-md-loads-only-in-scope` — the keep-vs-move test. Nested `CLAUDE.md`
  loads *only* under its directory, so a rule binding other modules stays in root even when a
  nested file repeats it. This is why Bean wiring / Repository layout / Response & paging shapes /
  User identity / Gateway routing were **kept** despite looking like duplicates.

## Remaining
- The `CLAUDE.md` restructure is **uncommitted** — review `git diff`, then commit.
- Out-of-repo (personal machine, tracked in global auto-memory, not here):
  46 broken `~/.claude/skills/*` gstack symlinks still need deleting; the guard hook blocks `rm`,
  so the user must run it. Two unused plugins were disabled in `~/.claude/settings.json`.
- Unchanged from before: manual keyboard/focus-ring a11y pass; consistency-loop R1 smoke test.
