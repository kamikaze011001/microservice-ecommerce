# Session 2026-07-24 — AWS EKS live bring-up, Step-3 storage stall fixed

## Done
- Diagnosed (systematic-debugging) why `make aws-all` died at Step 3/9: the infra statefulsets name
  no `storageClassName`, a fresh EKS defaults to gp2, and NOTHING in the aws-all flow applied our gp3
  SC → PVCs Pending → `rollout status` timeout → `set -e` aborted the whole script before SR/Connect/
  VM/Grafana/CDC. Live-patched (gp3 apply + gp2 demote; RetroactiveDefaultSC bound the pending PVCs) →
  mongodb-0 2/2, kafka-0 1/1.
- **Durable fix:** wired the gp3 SC apply + gp2 demotion into `scripts/aws/infra-up.sh` (after
  namespaces, before any PVC). Idempotent.
- Verified `aws/main/rds.tf` replica fix (`create_db_*_group = false`, lines 127–128) is on disk.
- Confirmed exact remaining state: Step 3 tail + Steps 4–9 all still pending.

## Decisions
- Resume with full idempotent `make aws-all` (not a partial step) — safest, no out-of-order risk.
- Keep `set -euo pipefail` fail-fast in infra-up.sh — the guard was right; the SC gap was the bug.

## Remaining
- USER runs `make aws-all` to finish Step 3 tail → Step 9 (watch Step 6 apps @ 600s cold pulls).
- Then walk the saga at `https://shop.microecom.click`; fix parity gaps; teardown + leak-check.
- Decide commit timing for the two working-tree fixes (infra-up.sh, rds.tf).
