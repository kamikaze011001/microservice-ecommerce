apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: apps
  labels: { app: order-service }
spec:
  type: ClusterIP
  selector: { app: order-service }
  ports:
    - { name: http, port: 9696, targetPort: http }
    - { name: management, port: 19696, targetPort: management }
