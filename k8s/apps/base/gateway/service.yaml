apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: apps
  labels: { app: gateway }
spec:
  type: ClusterIP
  selector: { app: gateway }
  ports:
    - { name: http, port: 6868, targetPort: http }
    - { name: management, port: 19093, targetPort: management }
