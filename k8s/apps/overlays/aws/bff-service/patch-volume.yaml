apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-service
  namespace: apps
spec:
  template:
    spec:
      containers:
        - name: bff-service
          volumeMounts:
            - { name: app-config, mountPath: /etc/app-config, readOnly: true }
      volumes:
        - name: app-config
          secret:
            secretName: bff-service-config
