apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: apps
spec:
  template:
    spec:
      containers:
        - name: inventory-service
          volumeMounts:
            - { name: app-config, mountPath: /etc/app-config, readOnly: true }
      volumes:
        - name: app-config
          secret:
            secretName: inventory-service-config
