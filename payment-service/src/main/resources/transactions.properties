com.atomikos.icatch.log_base_name = payment-service
# Headroom over the default 50: PayPal HTTP no longer runs inside a tx
# (call-then-record), but peak write bursts still need slots (stress 2026-06-10).
com.atomikos.icatch.max_actives = 200